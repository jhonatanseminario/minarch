shadow = true;
shadow-radius = 4;
shadow-offset-x = 0;
shadow-offset-y = 0;

backend = "xrender";
vsync = true;
detect-transient = true;
use-damage = true;

rules: ({
  match = "override_redirect = true || "
          "_GTK_FRAME_EXTENTS@";
  shadow = false;
})
